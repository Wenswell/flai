# Context Policy

Use project-level `.flai/now.md`, `.flai/project.md`, and `.flai/context-policy.md` first.

Do not read task logs by default.
Do not write docs by default.
Update `.flai` only when the change affects future work.
